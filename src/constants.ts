export const OPERATIONS_REGEX = /[+\-*/]/;
export const OPERATIONS_REGEX_WITH_OPERATORS = /[A-Za-z0-9]+|[+\-*/]/g;

export const DEFAULT_ROWS = 10;
export const DEFAULT_COLUMNS = 10;
export const MAX_ROWS = 100;
export const MAX_COLUMNS = 100;
